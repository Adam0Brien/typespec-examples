// Generated by Microsoft TypeSpec

syntax = "proto3";

package resources.v1;

enum ClusterStatus {
  CLUSTER_STATUS_UNSPECIFIED = 0;
  CLUSTER_STATUS_OTHER = 1;
  READY = 2;
  FAILED = 3;
  OFFLINE = 4;
}

enum KubeVendor {
  KUBE_VENDOR_UNSPECIFIED = 0;
  KUBE_VENDOR_OTHER = 1;
  AKS = 2;
  EKS = 3;
  IKS = 4;
  OPENSHIFT = 5;
  GKE = 6;
}

message K8sCluster {
  string external_cluster_id = 219571597;
  string kube_version = 395858490;
  ClusterStatus cluster_status = 395858492;
  KubeVendor kube_vendor = 395858494;
}

enum HostStatus {
  ENABLED = 0;
  DISABLED = 1;
}

// Represents RHEL host resource data.
message RhelHost {
  string host_id = 219571597;
  string os_version = 395858490;
  HostStatus host_status = 395858490;
}

// Create resource request.
message CreateResourceRequest {
  RhelHost rhel_host = 1;
  K8sCluster k8s_cluster = 2;
}

// Create resource response.
message CreateResourceResponse {}

// Update resource request.
message UpdateResourceRequest {
  RhelHost rhel_host = 1;
  K8sCluster k8s_cluster = 2;
}

// Update resource response.
message UpdateResourceResponse {}

// Delete resource request.
message DeleteResourceRequest {
  RhelHost rhel_host = 1;
  K8sCluster k8s_cluster = 2;
}

// Delete resource response.
message DeleteResourceResponse {}

service KesselResourceService {
  // Create a new resource.
  rpc CreateResource(CreateResourceRequest) returns (CreateResourceResponse);
  // Update an existing resource.
  rpc UpdateResource(UpdateResourceRequest) returns (UpdateResourceResponse);
  // Delete a resource.
  rpc DeleteResource(DeleteResourceRequest) returns (DeleteResourceResponse);
}
