# **TypeSpec to Protobuf Converter**

This repository converts TypeSpec definitions into `.proto` files for Protobuf APIs.

## **Getting Started**

Run the `run_typespec.sh` script to generate protofiles to `tsp-output`


